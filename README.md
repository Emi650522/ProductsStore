# ProductsStore
Esta API provee operaciones básicas para gestión de productos, autenticación con JWT y un diseño basado en separación de capas (controllers, services, models).
Además, utiliza Firebase Firestore como base de datos NoSQL.

Node.js + Express

Firebase Admin SDK

Firestore (Base de datos)

JSON Web Tokens (JWT) para autenticación

Estructura por capas:
Controllers
Services
Models
Routes

Correr la Api
git clone (https://github.com/Emi650522/ProductsStore.git)
cd ProductsStore

npm install

npm start

Si necesitás soporte o querés extender la API, contactame

*********************************************************

This API provides basic product management operations, JWT authentication, and a layered architecture (controllers, services, models).
It uses Firebase Firestore as a NoSQL database and is built with Node.js + Express.

Technologies Used

Node.js + Express

Firebase Admin SDK

Firestore (NoSQL Database)

JSON Web Tokens (JWT) for authentication

Layered architecture:

Controllers
Services
Models
Routes

# Clone repository
git clone https://github.com/Emi650522/ProductsStore.git
cd ProductsStore

# Install Dependencies
npm install

# Start the server
npm start

# The api will run on
http://localhost:3000

Contact

If you need support or want to extend the API, feel free to reach out.



